# Streamlit web app
streamlit>=1.25.0

# Ultralytics YOLO (model API)
ultralytics>=8.0.0

# PyTorch (install separately if needed; pin to appropriate build for your environment)
torch
torchvision

# Use headless OpenCV (no system libGL dependency)
opencv-python-headless==4.7.0.72

# Image processing, canvas, numerical
Pillow>=9.0.0
numpy>=1.23.0

# Streamlit canvas widget
streamlit-drawable-canvas>=0.9.0

# FFmpeg helper (bundled ffmpeg executable for many platforms)
imageio-ffmpeg>=0.4.8

# optional utilities used in scripts
imageio>=2.9.0